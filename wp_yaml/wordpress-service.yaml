apiVersion: v1
kind: Service
metadata:
  name: wordpress-service
  namespace: wp-ns
  labels:
    app: wordpress
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: wordpress
